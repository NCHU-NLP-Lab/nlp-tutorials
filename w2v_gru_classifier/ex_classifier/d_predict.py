from .a_sent2vec import sent2vec
from .c_model import SentClassifier
import torch
from sklearn.preprocessing import LabelEncoder
import pickle

# load 模型
# load LabelEncoder 規則

def sent_predictor(input=''):

	# 將輸入轉成 input 格式丟入模型，並使用 LabelEncoder 規則將預測結果轉乘類別名稱

    return 'predicted_class'
